% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_files.R
\name{upload_file_chunked}
\alias{upload_file_chunked}
\title{Upload a file using chunked upload}
\usage{
upload_file_chunked(
  config,
  file_path,
  filename = NULL,
  chunk_size_mb = 10,
  content_type = "application/octet-stream"
)
}
\arguments{
\item{config}{API configuration created by create_api_config}

\item{file_path}{Path to the file to upload}

\item{filename}{Name to use for the uploaded file (default: basename of file_path)}

\item{chunk_size_mb}{Size of each chunk in megabytes (default: 10)}

\item{content_type}{MIME type of the content (default: "application/octet-stream")}
}
\value{
TRUE if the upload was successful
}
\description{
Upload a file using chunked upload
}
\details{
This function uploads large files in chunks without loading the entire file
into memory. It:
\enumerate{
\item Calculates the file hash efficiently
\item Initializes a chunked upload session
\item Reads and uploads the file in chunks
\item Finalizes the upload and verifies the hash
}

The function includes progress reporting and automatic retry for failed chunks.
}
\examples{
\dontrun{
config <- create_api_config("http://localhost", 9000, "please_change_me")
upload_file_chunked(config, "large_dataset.rds", chunk_size_mb = 10)
}
}
