% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unit.R
\name{ds.hpc.unit.init}
\alias{ds.hpc.unit.init}
\title{Select an HPC execution unit for the DataSHIELD session}
\usage{
ds.hpc.unit.init(
  conns,
  resource = getOption("dshpc.unit.resource", NULL),
  symbol = "hpc_unit"
)
}
\arguments{
\item{conns}{A DSI connection or a named list of DSI connections.}

\item{resource}{Resource name, or an exact per-site named vector/list.}

\item{symbol}{Visible server symbol for the opaque unit handle.}
}
\value{
\code{TRUE}, invisibly.
}
\description{
Assigns an Opal or Armadillo Resource through the DSI generic and replaces
it with an opaque, session-bound dsHPC handle. Later domain workflows in the
same server session are pinned to this unit when they submit durable jobs.
}
\details{
\code{resource} defaults to the client-side option \code{dshpc.unit.resource}. A
scalar name is used at every site; a named vector/list must contain exactly
one Resource name for every connection. Resource names are opaque, so both
Opal's \code{PROJECT.resource} convention and Armadillo's
\code{project/folder/name} identifiers are accepted.
}
