% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08_compress.R
\name{compress_content}
\alias{compress_content}
\title{Compress content deterministically}
\usage{
compress_content(content, type = c("gzip", "xz", "bzip2"))
}
\arguments{
\item{content}{Content to compress (raw vector, character, or other object)}

\item{type}{Compression type: "xz" (default, best compression), "gzip" (faster), or "bzip2"}
}
\value{
Raw vector with compressed content (deterministic - same input always produces same output)
}
\description{
Compress content deterministically
}
\details{
This function provides deterministic compression, meaning that compressing the same
content multiple times will always produce the same compressed output and hash.
This is essential for content-based caching systems.

Compression types:
\itemize{
\item "xz": Best compression ratio, deterministic, slower (recommended for large files)
\item "gzip": Good compression, deterministic, faster (recommended for general use)
\item "bzip2": Medium compression, deterministic
}
}
\examples{
\dontrun{
content <- "Hello, World!"
compressed <- compress_content(content, type = "xz")
hash1 <- hash_content(compressed)
Sys.sleep(5)
compressed2 <- compress_content(content, type = "xz")
hash2 <- hash_content(compressed2)
identical(hash1, hash2)  # TRUE
}
}
