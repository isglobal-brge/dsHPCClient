% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09_sorting_utils.R
\name{sort_chain}
\alias{sort_chain}
\title{Sort method chain parameters}
\usage{
sort_chain(chain)
}
\arguments{
\item{chain}{List of method steps, each with method_name and parameters}
}
\value{
Chain with all parameters recursively sorted
}
\description{
Sort all parameters within a method chain.
The chain order itself is preserved (it's sequential),
but parameters within each step are sorted recursively.
}
\examples{
chain <- list(
  list(method_name = "concat", parameters = list(b = "2", a = "1")),
  list(method_name = "process", parameters = list(z = 10, x = 5))
)
sorted <- sort_chain(chain)
}
