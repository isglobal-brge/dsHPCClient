% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wait.R
\name{ds.hpc.wait}
\alias{ds.hpc.wait}
\title{Wait for a dsHPC job to reach a terminal state}
\usage{
ds.hpc.wait(conns, job_id, timeout = 3600, poll_interval = 5)
}
\arguments{
\item{conns}{DSI connections object.}

\item{job_id}{Character; opaque bearer or domain workflow symbol (see
\code{ds.hpc.job_id()}).}

\item{timeout}{Numeric timeout in seconds.}

\item{poll_interval}{Numeric polling interval in seconds.}
}
\value{
A \code{dshpc_result} status object from \code{ds.hpc.status()}.
}
\description{
Wait for a dsHPC job to reach a terminal state
}
\examples{
\dontrun{
# conns <- DSI::datashield.login(...)  # live DataSHIELD session
ids <- ds.hpc.job_id(conns, "jobA")
st <- ds.hpc.wait(conns, ids[[1]], timeout = 600, poll_interval = 5)
print(st)
}
}
