% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outputs.R
\name{ds.hpc.load_output}
\alias{ds.hpc.load_output}
\title{Assign a shared output reference in the server session}
\usage{
ds.hpc.load_output(conns, tracking_id, output_name, symbol = NULL)
}
\arguments{
\item{conns}{DSI connections object.}

\item{tracking_id}{Public scalar \code{trk_...} id, or a vector naming one id for
every connection.}

\item{output_name}{Fixed public alias returned by \code{\link[=ds.hpc.outputs]{ds.hpc.outputs()}}:
\code{output_001} or \code{output_002}.}

\item{symbol}{Optional visible R symbol to create in the server sessions.
By default a valid symbol is derived from \code{output_name}.}
}
\value{
\code{TRUE}, invisibly. No output value is downloaded.
}
\description{
Creates an opaque \code{dshpc_output_reference} in each selected server session.
The output value, artifact path and underlying execution job never cross to
the client. Trusted server packages can consume the reference in later
pipelines, and every client-facing result remains subject to disclosure
control.
}
\examples{
\dontrun{
ds.hpc.load_output(conns["site1"], "trk_...", "output_001",
  symbol = "shared_features")
}
}
